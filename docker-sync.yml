version: "2"

options:
  verbose: false
  max-attempt: 30
syncs:
  nuxeo-${APPLICATION_NAME}-sync:
    src: './nuxeo.war'
    sync_userid: 1000
    sync_strategy: 'unison'
    # sync_excludes: ['ignored_folder', '.ignored_dot_folder']

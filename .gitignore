# Source environment config - **autogenerated**
.env
env

# Custom nuxeo packages (optional): ignore the .zip payloads but keep the folder
# (needed by the Docker build context) and its README.
nuxeo_packages/*
!nuxeo_packages/README.md

# Local server configuration
conf

# Custom Nuxeo Log configuration
init/log4j2.xml

# Optional folders to sync (see docker-compose.yml)
nuxeo_store
nuxeo_war

# Stuff for dev
.yo-rc.json
sh.sh

# Other
.DS_Store